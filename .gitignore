__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
.DS_Store

# Maintainer-private files: real paths, endpoints, channel lists.
# Never commit. See CLAUDE.md.
MAINTENANCE.local.md
*.local.md
*.local.yaml

# Legacy local replica-script remnants are not tracked or invoked by Backchannel.
# Keep them ignored so a migration does not stage machine-specific render output.
index/sync_pull.sh
index/update.sh
index/fetch_now.sh
index/monthly_freeze.sh
index/monthly_rolling.sh
build/
.playwright-cli/
# roborev snapshots
/.roborev/
